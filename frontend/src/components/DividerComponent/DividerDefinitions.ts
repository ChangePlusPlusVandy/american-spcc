/**
 * Divider component type definitions
 *
 * Defines the interface (props) for the Divider component.
 * Specifies what props Divider accepts.
 */
export interface DividerProps {
  text?: string;
}
